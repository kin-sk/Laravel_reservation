# 本日の作業まとめ（環境構築）

## 概要

Laravelアプリケーションの開発環境を、devcontainer + Docker構成で構築し、ブラウザからの動作確認まで完了した。

---

## 実施内容

### 1. 開発環境構築（devcontainer）

- `.devcontainer` + `docker-compose.yml` を使用して開発環境を構築
- PHP（php-fpm）、Nginx、PostgreSQLの3コンテナ構成を作成
- ソースコード（src）をコンテナへマウント

---

### 2. PHPコンテナ設定

- `docker/php/Dockerfile` を作成
- PostgreSQL接続用に `pdo_pgsql` をインストール
- Composerを導入
- 作業ディレクトリを `/workspace/src` に設定

---

### 3. Nginx設定

- `docker/nginx/default.conf` を作成
- Laravelのエントリポイントとして `public` ディレクトリを指定
- FastCGIでphp-fpm（app:9000）へ接続

#### 修正ポイント

- rootパスの誤りを修正
  - 修正前: `/var/www/src/public`
  - 修正後: `/workspace/src/public`

---

### 4. 環境変数設定（.env）

- DB接続をPostgreSQLへ変更
- 開発用設定として以下を適用
  - `SESSION_DRIVER=file`
  - `CACHE_DRIVER=file`
  - `QUEUE_CONNECTION=sync`

- `APP_KEY` を生成

---

### 5. データベース構築

- PostgreSQLコンテナ起動
- `php artisan migrate` 実行
- 全マイグレーション成功（users, reservations など）

---

### 6. 動作確認

- ブラウザ（[http://localhost:8080）でアクセス確認](http://localhost:8080）でアクセス確認)
- Laravelの画面表示を確認
- `php artisan tinker` にてDB接続確認（User::all()）

---

## 問題と対応

### 問題①

- cacheテーブルが存在しないエラー

**対応**

- `.env` の CACHE設定を `file` に変更

---

### 問題②

- 502 Bad Gateway（nginx）

**原因**

- php-fpm未起動 / パス不一致

---

### 問題③

- "File not found"

**原因**

- nginxのrootパスとvolumeのマウント先が不一致

**対応**

- `/workspace/src/public` に修正

---

### 問題④

- APP_KEY未設定エラー

**対応**

- `php artisan key:generate` 実行

---

## 現在の構成

- Laravel（PHP-FPM）
- Nginx
- PostgreSQL
- devcontainer（Docker Compose）

---

## 今後の予定

- devcontainer内の開発環境整備（Git / Node / VSCode拡張）
- フロントエンド環境構築（Vite / Node）
- APIおよびCRUD機能の実装
- Next.jsとの連携

---

## 補足

開発環境と本番環境でコンテナ構成およびパスが異なるため、設定の混在に注意すること。
